<main>
  <div class="container">
    <div class="row">
      <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
        <h2>投稿を編集</h2>
        <div id="error_explanation">
          <% if @post_anime.errors.any? %>
            <h3>
              <%= @post_anime.errors.count %> 件のエラーが発生しました
            </h3>
            <ul>
              <% @post_anime.errors.full_messages.each do |message| %>
              <li>
                <%= message %>
              </li>
              <% end %>
            </ul>
          <% end %>
        </div>
        <%= form_with model: @post_anime, local:true do |f| %>
          <div class="form-group">
            <%= f.label :title, 'タイトル' %><br>
            <%= f.text_field :title, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :body, 'レビュー %><br>
            <%= f.text_area :body, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.submit '完了', class: "btn btn-success" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</main>